1. download git
2. open github
3. create repo
4. create a new file
5. commit changes
6. update any changes if any
7. open VS code
8. open terminal
9. write the following commands on your terminal
    git config --global user.name "Datameow07"
    git config --global user.email "arbiazabi3094@gmail.com"
    git clone https://github.com/Datameow07/DevOps.git 
    (you can find the link in your repo code and it will be in the form fo https)
10. write the command 
     cd DevOps (to change the directory)
     ls -lrt (to show the when was the file created and its details)
11. making use of these commands
     git add prod.txt
     git add .
     git commit -m "first commit"
     git push origin main
     (then the file will be saved on github)
12. if you make changes in github and commit changes then in order to bring the content on vscode
     git pull origin main

google meet code: vus-rebp-zrf